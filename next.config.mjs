/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: [
      "images.unsplash.com",
      "www.pexels.com",
      "plus.unsplash.com",
      "res.cloudinary.com",
    ],
  },
};

export default nextConfig;
