export const metadata = {
  title: "Carousel Management",
  description: "Carousel Management",
};

export default function CarouselManagementLayout({ children }) {
  return <div className={``}>{children}</div>;
}
